pipeline {
    agent any {
        stage('Run tests') {
            steps {
                sh '''
                    rostopic pub /drone/land std_msgs/Empty '{}' &
                    LAND_ID=$!
                    sleep 5s
                    kill $LAND_ID
                '''
            }
        }
    }
}